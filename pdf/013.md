Let's go, Aung. ဒီ Lesson မှာ အရေးကြီးတဲ့ concept တစ်ခုကို **ပြန်တည့်ပေးပြီး** actual implementation သွားမယ်။

အရင် lessons တွေမှာ `JFreeChart → PDFBox` ကို တိုက်ရိုက် `Graphics2D` နဲ့ဆွဲနိုင်သလို ရေးထားတာရှိတယ်။ **PDFBox ကိုယ်တိုင်က `Graphics2D` renderer မဟုတ်ပါဘူး။** JFreeChart ရဲ့ vector PDF ကို OrsonPDF က generate လုပ်ပေးနိုင်ပြီး၊ PDFBox ရဲ့ `LayerUtility` က အခြား PDF page ကို `Form XObject` အဖြစ် import လုပ်ပြီး target page ပေါ်မှာ place လုပ်နိုင်ပါတယ်။ ဒါကြောင့် **vector chart + PDFBox report** ကို အကောင်းဆုံးနားလည်ရမယ့် architecture က ဒီလိုပါ။ ([JFree](https://jfree.github.io/?utm_source=chatgpt.com "David Gilbert – Java Developer / Creator of http://www.jfree.org"))

# Lesson 6 — JFreeChart Vector PDF + PDFBox Report

## 1. Final Architecture

```text
                    Student Data
                         │
                         ▼
                ┌─────────────────┐
                │  JFreeChart     │
                └────────┬────────┘
                         │
                         ▼
                  OrsonPDF
                         │
                         ▼
               Vector Chart PDF
                         │
                         │
                         ▼
┌─────────────────────────────────────────┐
│              PDFBox Report              │
│                                         │
│  Header                                 │
│  Summary                                │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │       VECTOR JFREECHART            │  │
│  └───────────────────────────────────┘  │
│                                         │
│  Student Table                          │
│                                         │
│  Footer                                 │
└─────────────────────────────────────────┘
```

ဒီမှာ—

- **JFreeChart** → chart ဖန်တီး
    
- **OrsonPDF** → chart ကို vector PDF အဖြစ် render
    
- **PDFBox** → report page, text, table, footer စတာတွေ manage
    
- **LayerUtility** → chart PDF page ကို PDFBox report page ထဲ import
    

PDFBox `LayerUtility.importPageAsForm()` က source PDF page ကို `PDFormXObject` အဖြစ် import လုပ်နိုင်ပြီး `drawForm()` နဲ့ target page ပေါ်မှာ draw လုပ်နိုင်ပါတယ်။ ([Apache PDFBox](https://pdfbox.apache.org/docs/2.0.5/javadocs/org/apache/pdfbox/multipdf/LayerUtility.html?utm_source=chatgpt.com "LayerUtility (PDFBox reactor 2.0.5 API)"))

---

# 2. Maven Dependencies

ဒီ lesson အတွက် PDFBox ကိုပါထည့်မယ်။

```xml
<dependencies>

    <dependency>
        <groupId>org.jfree</groupId>
        <artifactId>jfreechart</artifactId>
        <version>1.5.6</version>
    </dependency>

    <dependency>
        <groupId>com.orsonpdf</groupId>
        <artifactId>orsonpdf</artifactId>
        <version>1.9.1</version>
    </dependency>

    <dependency>
        <groupId>org.apache.pdfbox</groupId>
        <artifactId>pdfbox</artifactId>
        <version>2.0.32</version>
    </dependency>

</dependencies>
```

ဒီ lesson မှာ **PDFBox 2.x API** ကိုသုံးမယ်။ `LayerUtility` ရဲ့ `importPageAsForm()` API က PDFBox 2.x documentation မှာ တိတိကျကျရှိပါတယ်။ ([Apache PDFBox](https://pdfbox.apache.org/docs/2.0.5/javadocs/org/apache/pdfbox/multipdf/LayerUtility.html?utm_source=chatgpt.com "LayerUtility (PDFBox reactor 2.0.5 API)"))

> နောက်ပိုင်း PDFBox 3.x သုံးချင်ရင် API differences ကို သီးခြား lesson တစ်ခုအနေနဲ့ပြောင်းသင်မယ်။ အခု lesson မှာ concept ကိုအရင် solid ဖြစ်အောင်လုပ်မယ်။

---

# 3. ပထမဆုံး JFreeChart တစ်ခုဖန်တီး

```java
public JFreeChart createMonthlyChart() {

    DefaultCategoryDataset dataset =
            new DefaultCategoryDataset();

    dataset.addValue(
            80,
            "Students",
            "Jan"
    );

    dataset.addValue(
            120,
            "Students",
            "Feb"
    );

    dataset.addValue(
            150,
            "Students",
            "Mar"
    );

    dataset.addValue(
            180,
            "Students",
            "Apr"
    );

    dataset.addValue(
            210,
            "Students",
            "May"
    );

    return ChartFactory.createBarChart(
            "Monthly Student Registration",
            "Month",
            "Students",
            dataset
    );
}
```

အခု—

```text
JFreeChart
    │
    └── Monthly Registration
```

ရပြီ။

---

# 4. JFreeChart ကို Vector PDF အဖြစ် generate

OrsonPDF ရဲ့ အဓိက idea က `Graphics2D` API ကို implement လုပ်ပြီး Java2D drawing operations ကနေ PDF bytes ထုတ်တာပါ။ ဒါကြောင့် chart ကို PNG အဖြစ် rasterize လုပ်တာနဲ့ မတူပါဘူး။ ([JFree](https://jfree.github.io/?utm_source=chatgpt.com "David Gilbert – Java Developer / Creator of http://www.jfree.org"))

`VectorChartPdf.java`

```java
package com.ams.report.chart;

import com.orsonpdf.PDFDocument;
import com.orsonpdf.PDFGraphics2D;
import com.orsonpdf.Page;

import org.jfree.chart.JFreeChart;

import java.awt.Rectangle;
import java.io.File;
import java.io.FileOutputStream;
import java.io.IOException;

public class VectorChartPdf {

    public static void export(
            JFreeChart chart,
            File outputFile
    ) throws IOException {

        int width = 700;
        int height = 350;

        PDFDocument pdf =
                new PDFDocument();

        Page page =
                pdf.createPage(
                    new Rectangle(
                        width,
                        height
                    )
                );

        PDFGraphics2D graphics =
                page.getGraphics2D();

        chart.draw(
                graphics,
                new Rectangle(
                    0,
                    0,
                    width,
                    height
                )
        );

        try (FileOutputStream out =
                     new FileOutputStream(
                         outputFile
                     )) {

            out.write(
                pdf.getPDFBytes()
            );
        }
    }
}
```

ဒီ method ကို run လိုက်ရင်—

```text
monthly-chart.pdf
```

ရမယ်။

---

# 5. ဒါဆို PDFBox ဘာလုပ်မလဲ?

အခု—

```text
monthly-chart.pdf
```

ရှိပြီ။

PDFBox report က—

```text
student-report.pdf
```

ဖြစ်မယ်။

ကျွန်တော်တို့လုပ်ချင်တာ—

```text
monthly-chart.pdf
        │
        │ import
        ▼
student-report.pdf
```

ပါ။

---

# 6. `LayerUtility` ကိုသုံးမယ်

PDFBox ရဲ့—

```java
LayerUtility
```

က source PDF page ကို—

```java
PDFormXObject
```

အဖြစ် import လုပ်နိုင်တယ်။ ([Apache PDFBox](https://pdfbox.apache.org/docs/2.0.5/javadocs/org/apache/pdfbox/multipdf/LayerUtility.html?utm_source=chatgpt.com "LayerUtility (PDFBox reactor 2.0.5 API)"))

အဲ့ဒီအတွက်—

```java
LayerUtility layerUtility =
        new LayerUtility(
            targetDocument
        );
```

ပြီးတော့—

```java
PDFormXObject chartForm =
        layerUtility.importPageAsForm(
            chartDocument,
            0
        );
```

ဖြစ်မယ်။

---

# 7. `PDFormXObject` ဆိုတာဘာလဲ?

ဒီ concept ကိုသေချာနားလည်ပါ။

PDF မှာ reusable drawing object တစ်ခုလိုစဉ်းစားလို့ရတယ်။

```text
PDF
│
├── Text
├── Images
├── Shapes
└── Form XObject
       │
       └── Chart drawing
```

အဲ့ဒီ `Form XObject` ကို—

```java
content.drawForm(chartForm);
```

နဲ့ page ပေါ်မှာ ထည့်နိုင်တယ်။ PDFBox documentation မှာ `drawForm(PDFormXObject)` က Form XObject ကို current location မှာ draw လုပ်တာဖြစ်ပါတယ်။ ([Apache PDFBox](https://pdfbox.apache.org/docs/2.0.2/javadocs/org/apache/pdfbox/pdmodel/PDPageContentStream.html?utm_source=chatgpt.com "PDPageContentStream (PDFBox reactor 2.0.2 API)"))

---

# 8. အရေးကြီးတဲ့ coordinate problem

PDF coordinate system က—

```text
(0,0)
  └──────────────→ X
  │
  │
  │
  ↓
  Y
```

အများအားဖြင့် bottom-left origin ဖြစ်တယ်။

ဒါပေမယ့် chart layout ကိုရေးတဲ့အခါ—

```text
Top
 ↓
Chart
 ↓
Bottom
```

လို စဉ်းစားရတယ်။

ဒါကြောင့် chart placement အတွက် `AffineTransform` သုံးမယ်။

---

# 9. Chart ကို target page မှာ scale လုပ်မယ်

ဥပမာ target page မှာ chart area—

```text
x = 40
y = 260

width  = 760
height = 280
```

ဆိုပါစို့။

Chart source size—

```text
700 × 350
```

ဆိုရင် scale—

```text
scaleX = 760 / 700
scaleY = 280 / 350
```

တွက်ရမယ်။

---

# 10. `PdfChart` class

```java
package com.ams.report.pdf;

import com.ams.report.chart.VectorChartPdf;

import java.awt.geom.AffineTransform;
import java.io.File;
import java.io.IOException;

import org.apache.pdfbox.multipdf.LayerUtility;
import org.apache.pdfbox.pdmodel.PDDocument;
import org.apache.pdfbox.pdmodel.PDPage;
import org.apache.pdfbox.pdmodel.graphics.form.PDFormXObject;

public class PdfChart {

    public static void draw(
            PDDocument targetDocument,
            PDPage targetPage,
            File chartPdf,
            float x,
            float y,
            float width,
            float height
    ) throws IOException {

        try (
            PDDocument chartDocument =
                    PDDocument.load(chartPdf)
        ) {

            LayerUtility utility =
                    new LayerUtility(
                        targetDocument
                    );

            PDFormXObject chartForm =
                    utility.importPageAsForm(
                        chartDocument,
                        0
                    );

            float sourceWidth = 700;
            float sourceHeight = 350;

            float scaleX =
                    width / sourceWidth;

            float scaleY =
                    height / sourceHeight;

            AffineTransform transform =
                    new AffineTransform();

            transform.translate(
                    x,
                    y
            );

            transform.scale(
                    scaleX,
                    scaleY
            );

            utility.appendFormAsLayer(
                    targetPage,
                    chartForm,
                    transform,
                    "JFreeChart"
            );
        }
    }
}
```

ဒါက—

```text
chart PDF
   ↓
PDFormXObject
   ↓
scale
   ↓
position
   ↓
target PDF page
```

ဖြစ်သွားတာ။

---

# 11. အခု Full Report

အခု report page ကို—

```java
PDDocument document =
        new PDDocument();

PDPage page =
        new PDPage(
            PDRectangle.A4
        );

document.addPage(page);
```

ဖန်တီးမယ်။

---

# 12. Header

```java
try (
    PDPageContentStream content =
        new PDPageContentStream(
            document,
            page
        )
) {

    content.beginText();

    content.setFont(
        PDType1Font.HELVETICA_BOLD,
        20
    );

    content.newLineAtOffset(
        40,
        550
    );

    content.showText(
        "STUDENT MANAGEMENT SYSTEM"
    );

    content.endText();
}
```

---

# 13. Summary

```java
try (
    PDPageContentStream content =
        new PDPageContentStream(
            document,
            page
        )
) {

    content.beginText();

    content.setFont(
        PDType1Font.HELVETICA,
        11
    );

    content.newLineAtOffset(
        40,
        520
    );

    content.showText(
        "Total Students: 740"
    );

    content.endText();
}
```

---

# 14. Chart placement

အခု `PdfChart.draw()` ကိုခေါ်မယ်။

```java
File chartPdf =
        new File(
            "monthly-chart.pdf"
        );
```

ပြီးရင်—

```java
PdfChart.draw(
    document,
    page,
    chartPdf,
    40,
    220,
    515,
    250
);
```

ဆိုရင် chart က—

```text
              Page
┌─────────────────────────────┐
│ Header                      │
│                             │
│ Total Students: 740         │
│                             │
│ ┌─────────────────────────┐ │
│ │                         │ │
│ │       JFreeChart        │ │
│ │                         │ │
│ └─────────────────────────┘ │
│                             │
└─────────────────────────────┘
```

ဖြစ်လာမယ်။

---

# 15. Table ကို အောက်မှာထားမယ်

Chart အောက်မှာ—

```java
PdfTable table =
        new PdfTable(
            40,
            190,
            25,
            new float[]{
                80,
                180,
                100,
                150
            },
            headers,
            rows
        );
```

ဒါပေမယ့် ဒီနေရာမှာ **အရေးကြီးတဲ့ issue** တစ်ခုရှိတယ်။

Chart height က dynamic ဖြစ်နိုင်တယ်။

Table rows က dynamic ဖြစ်နိုင်တယ်။

ဒါကြောင့်—

```text
Header
 ↓
Summary
 ↓
Chart
 ↓
Table
 ↓
Footer
```

ကို fixed coordinates နဲ့ အမြဲရေးတာက professional architecture မဟုတ်တော့ဘူး။

---

# 16. Layout Manager လိုလာပြီ

အခု architecture ကို—

```text
PdfPageManager
```

တစ်ခုတည်းနဲ့မပြီးတော့ဘူး။

နောက်ထပ်—

```text
PdfLayout
```

လိုလာတယ်။

Concept:

```text
PdfLayout
│
├── currentY
├── margin
├── pageWidth
├── pageHeight
│
├── reserve(height)
├── moveDown(height)
└── newPage()
```

ဥပမာ—

```java
layout.reserve(250);
```

ဆိုရင်—

```text
space available?
    │
    ├── YES → reserve
    │
    └── NO → new page
```

---

# 17. ဒီလိုဖြစ်လာမယ်

```text
StudentReportPdf
       │
       ↓
   PdfLayout
       │
 ┌─────┼──────────────┐
 ↓     ↓              ↓
Text  Chart          Table
       │              │
       ↓              ↓
    JFreeChart     Pagination
       │              │
       └──────┬───────┘
              ↓
             PDF
```

ဒီ architecture က တကယ် project ထဲမှာသုံးလို့ကောင်းတယ်။

---

# 18. အရေးကြီးတဲ့ Vector concept

ဒီနေရာမှာ—

### ❌ PNG approach

```text
JFreeChart
   ↓
BufferedImage
   ↓
PNG
   ↓
PDFBox
   ↓
PDF
```

ဖြစ်ရင် chart က image ဖြစ်သွားတယ်။

Zoom အများကြီးလုပ်ရင်—

```text
████ pixels
```

တွေ့နိုင်တယ်။

### ✅ Vector approach

```text
JFreeChart
   ↓
Graphics2D
   ↓
OrsonPDF
   ↓
PDF vector content
   ↓
PDFormXObject
   ↓
PDFBox
```

ဖြစ်တယ်။

ဒါကြောင့် chart ရဲ့ lines, shapes, text စတာတွေကို PDF vector content အနေနဲ့ ထိန်းသိမ်းထားနိုင်တယ်။ OrsonPDF ရဲ့ design ကိုယ်တိုင်က Java2D `Graphics2D` API ကို implement လုပ်ပြီး PDF drawing commands/bytes ထုတ်ပေးတဲ့ approach ဖြစ်ပါတယ်။ ([JFree](https://jfree.github.io/?utm_source=chatgpt.com "David Gilbert – Java Developer / Creator of http://www.jfree.org"))

---

# 19. PDFBox ရဲ့ role ကို အခုရှင်းသွားပြီ

မင်းအရင်က—

> "JFreeChart ကို PDFBox နဲ့ ဘယ်လို print လုပ်မလဲ?"

လို့စဉ်းစားခဲ့တာ။

ပိုမှန်တဲ့ mental model က—

```text
              JFreeChart
                  │
                  │ chart rendering
                  ▼
               OrsonPDF
                  │
                  │ vector PDF
                  ▼
             PDFBox import
                  │
                  ▼
        ┌───────────────────┐
        │  Final PDF Report │
        └───────────────────┘
```

**PDFBox က JFreeChart renderer မဟုတ်ဘူး။**

PDFBox က **PDF document composition/manipulation layer** ဖြစ်တယ်။

---

# 20. Real Student Management System မှာ

မင်း project ကို ဒီလိုသွားနိုင်တယ်—

```text
StudentReportForm
       │
       │ click Export
       ▼
StudentReportService
       │
       ├── query MySQL
       │
       ├── build report data
       │
       └── create JFreeChart
                    │
                    ▼
              VectorChartPdf
                    │
                    ▼
              chart.pdf
                    │
                    ▼
             StudentReportPdf
                    │
             ┌──────┼──────┐
             ↓      ↓      ↓
           Header Chart   Table
                    │
                    ↓
               PDFBox
                    │
                    ▼
          student-report.pdf
```

---

## ဒီ Lesson မှာ မှတ်ထားရမယ့် 3 ချက်

**1. JFreeChart က chart ကိုဆွဲတယ်**

```java
chart.draw(...)
```

**2. OrsonPDF က chart ကို vector PDF အဖြစ် render လုပ်တယ်**

```text
Graphics2D → PDF
```

**3. PDFBox က အဲ့ဒီ PDF content ကို report ထဲ composition လုပ်တယ်**

```java
importPageAsForm(...)
```

ပြီးတော့—

```java
drawForm(...)
```

နဲ့ target page မှာနေရာချနိုင်တယ်။ ([Apache PDFBox](https://pdfbox.apache.org/docs/2.0.5/javadocs/org/apache/pdfbox/multipdf/LayerUtility.html?utm_source=chatgpt.com "LayerUtility (PDFBox reactor 2.0.5 API)"))

---

### နောက် Lesson — `PdfLayout`

အခုတော့ **တကယ် professional ဖြစ်ဖို့ `PdfLayout` class** ဆောက်မယ်။

အဲဒါနဲ့—

```text
Header
   ↓
Summary
   ↓
Chart
   ↓
Table
   ↓
Footer
```

တွေကို `y = 550`, `y = 500`, `y = 220` လို **hard-code မလုပ်တော့ဘဲ**—

```java
layout.drawText(...);
layout.drawChart(...);
layout.drawTable(...);
```

ဆိုတဲ့ flow နဲ့ automatic positioning + page break လုပ်နိုင်အောင် တည်ဆောက်မယ်။